# .NET 构建产物
bin/
obj/
.trx/
TestResults/

# IDE
.vscode/
.vs/
*.user

# Suite 状态产物 —— 不入库。再生它们只需要 `python scripts/gate.py -p <project>`。
.state/trace.json
.state/gate.json
.state/alignment.json
.state/gate.partial.json

# 本地环境覆盖（连接串等）—— 脚手架的 appsettings.Development.json 已入库，
# 真正的机密走环境变量/用户机密，不进 git。
appsettings.*.local.json

# Claude 临时文件
.claude/
.superpowers/
.mcp.json

# .env.production: 部署契约真值(含 secret),gitignored 不进 git(2026-08-28 起,同 .env.local 模式)
# key 集合由 suite L0.5 门核对(存在时与 .env.example 严格双向比对);真值经 scripts/sync-secrets 进 GitHub Secrets
.env.production
